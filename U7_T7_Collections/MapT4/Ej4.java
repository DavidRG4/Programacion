package com.company.MapT4;

import java.util.*;

public class Ej4 {
    /*public static void main(String[] args) {
        List<String> piezas=new ArrayList<>();
        List<Integer> peones=new ArrayList<>();

        int suma=0;
        peones.add(9);
        peones.add(5);
        peones.add(3);
        peones.add(2);
        peones.add(1);
        piezas.add("dama");
        piezas.add("torres");
        piezas.add("alfiles");
        piezas.add("caballos");
        piezas.add("peones");
        System.out.println("Piezas que capturo el jugador");
        for (int i = 0; i <((int)(Math.random()*16)) ; i++) {
           int pieza=(int) (Math.random()*5);
            System.out.println(piezas.get(pieza)+" vale= ("+peones.get(pieza)+" peones)");
            suma+= peones.get(pieza);
        }
        System.out.println("Puntos totales "+suma);
    }*/

    public static void main(String[] args) {
        Map<String,Integer> piezas=new  HashMap<>();
        for (int i = 0; i < ((int)(Math.random()*16)); i++) {

        }
    }
}
